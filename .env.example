# Configurações do Ambiente
# Copie este arquivo para .env e configure com seus valores

# Ambiente de execução (dev, hml, prd)
NODE_ENV=dev

# Porta do servidor HTTP
PORT=3333

# URL de conexão com o banco de dados SQLite
# Para desenvolvimento local, mantenha como está
DATABASE_URL="file:./dev.db"

# Para banco em produção, use algo como:
# DATABASE_URL="file:./prod.db"

# Para PostgreSQL (se necessário no futuro):
# DATABASE_URL="postgresql://username:password@localhost:5432/avantsoft_db"

# Configurações de Debug (opcional)
# DEBUG=prisma*
